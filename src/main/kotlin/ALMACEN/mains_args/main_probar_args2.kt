package ALMACEN.mains_args

fun main(args: Array<String>) {
    //ESTÁ BIEN HECHO PERO NO FUNCIONA
    //PARECE QUE SE RAYA CON LOS CONSTRUCTORES

    /*
    //para probar lo de args
    //COMPROBAMOS CONSTRUCTOR sin id:

    // litros precio marca identificador porcentaje_azucar promocion
    var litros=args[0].toDouble()
    var precio=args[1].toDouble()
    var marca=args[2]
    var porcentaje=args[3].toInt()
    var promocion=args[4].toBoolean()

    var sugar=Bebida_azucarada(litros, precio, marca, porcentaje, promocion )
    println(sugar.toString())
    */
}